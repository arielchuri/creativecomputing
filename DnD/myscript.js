$(document).ready(function(){ //Run the following after the page is ready.
    "use strict";             //Tell jQuery to be strict about the syntax.
    console.log("run");
    $("button").click(function() {
        console.log("pressed");
        // if ($("#myClass").val() == "Warrior"){
        //     window.location = "index.html"   
        // }
    }
    )


})
